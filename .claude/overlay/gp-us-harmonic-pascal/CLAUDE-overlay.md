# GP-US Harmonic Pascal — Project Overlay

> **Purpose.** This overlay file is loaded by the Claude Code runtime *after*
> root `AGENTS.md` and `.claude/CLAUDE.md`. It exists for **project-specific
> additions** that should NOT be promoted to Tier 1 (which is shared across
> projects that copy this `.claude/` template).
>
> If a section below is empty, that means the project hasn't needed a
> project-specific override yet. Adding content here is preferable to inflating
> `CLAUDE.md`.

## Project Identity

- Repo: gp-us-harmonic-pascal
- Display name: GP-US Harmonic Pascal
- Production URL: https://harmonic-pascal.grupous.com.br
- Stack: astro-static-tailwindv4
- Locale: pt-BR

## Cardinal Guards

*(none beyond the 8 cardinals in `.claude/CLAUDE.md § Cardinal rules`. Add a row
here when this project diverges from the generic stack — e.g. a forbidden
component pattern unique to this brand.)*

## Routing Supplements

*(none. Add a row here when a task type only applies to this project — e.g.
a custom CMS integration or a brand-specific landing variant.)*

## SEO Supplement

*(none beyond `.claude/rules/seo.md` + per-page frontmatter. Add JSON-LD
overrides or hreflang variants here if scope expands beyond pt-BR.)*
